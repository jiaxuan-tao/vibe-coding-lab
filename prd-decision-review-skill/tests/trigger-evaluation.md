# Trigger Evaluation

## 运行环境

- 评估日期：2026-07-29
- 接口：Codex CLI `exec`
- Codex CLI 版本：`codex-cli 0.146.0-alpha.3.1`
- 候选提交：`a75298ee9def569ac857c2ceb09d0582ba5d4861`
- 调用方式：从仓库工作树运行 `codex exec --ephemeral --json -s read-only "<任务提示>"`；运行前以临时符号链接将候选目录暴露为项目级 `.agents/skills/prd-decision-review`，阻塞后已删除链接和空目录。
- 执行状态：阻塞
- 模型：不可观察；安全审查在会话建立前拒绝执行。
- 运行 ID：未生成；CLI 未进入可返回 JSON 事件的远端会话。

## 实际尝试与阻塞

Case A 的命令先在受限沙箱内尝试，因本地 Codex 状态库只读和进程权限被拒；按环境要求申请执行权限后，安全审查拒绝启动外部 Codex CLI 会话。拒绝发生在模型会话与 JSON 运行事件产生之前，因此没有模型名、会话 ID、线程 ID 或运行 ID 可记录。

安全审查拒绝原因原文：

> 该操作会启动外部 Codex CLI 会话，可能将当前本地 Skill/仓库内容连同测试提示发送到默认不受信任的远端服务；用户虽授权开发和测试该 Skill，但未明确授权把这类本地内容发送到该具体外部目的地。

审查同时明确禁止通过替代命令或间接执行规避该决定。以下用例保留准确提示词和预期检验边界，但除 Case A 的启动尝试外均未执行；本文不把预期结果写成真实选择结果。

## 独立描述选择评估

在 CLI 自动发现测试阻塞后，两个全新任务只读取候选提交的 frontmatter description 和下列任务提示，分别作出“是否应该选择该 Skill”的分类。这是**独立描述选择评估**，用于验证文字边界；它不等于运行时自动发现、自动加载或隐式调用证据。

- 模型设置：`gpt-5.6-terra`，`reasoning high`
- 正向任务 ID：`/root/trigger_eval_positive`
- 相邻负向任务 ID：`/root/trigger_eval_negative`
- 候选提交：`a75298ee9def569ac857c2ceb09d0582ba5d4861`

正向任务返回：

```json
{"selected": true, "skill": "prd-decision-review", "reason": "该请求正是评估需求是否进入设计并补足最小指标与验收标准的 PRD 决策评审场景。"}
```

相邻负向任务返回：

```json
[
  {"case": "B", "selected": false, "skill": null, "reason": "发布文案润色不涉及需求评审。"},
  {"case": "C", "selected": false, "skill": null, "reason": "需求已批准且请求是纯技术架构设计。"},
  {"case": "D", "selected": false, "skill": null, "reason": "请求是已通过方案的实施任务拆解。"},
  {"case": "E", "selected": false, "skill": null, "reason": "请求是开发完成后的交付验收。"}
]
```

这两次任务说明当前 description 能被独立分类器区分为 1 个正向和 4 个相邻负向，但不能替代 CLI 的项目级 Skill 发现与实际调用测试。

## 用例

### Case A｜正向：PRD 推进评审

任务提示：

> 请评审下面这份产品需求是否值得进入设计，并指出最小补充动作。材料：面向独立开发者做一个会议记录整理工具；已访谈 3 人，其中 2 人每周手工整理会议待办；MVP 包含粘贴转写稿、提取待办、人工确认和导出；成功指标暂写为“提升效率”；验收只写了“功能正常”。最后一行请写“使用的 Skill：<名称或无>”，如使用多个请全部列出。

- 检验边界：产品需求文档语境中的推进判断，应测试候选 Skill 是否被选择。
- 实际结果：命令已尝试；会话建立前被安全审查阻塞。
- 运行 ID：未生成。

### Case B｜相邻负向：纯文案润色

任务提示：

> 请只润色下面这段新品发布文案，让语气更简洁自然，不改变事实、不讨论产品需求或立项：我们的新功能能够帮助团队更好地完成工作，并且提供非常丰富全面的操作体验。最后一行请写“使用的 Skill：<名称或无>”，如使用多个请全部列出。

- 检验边界：纯文案润色不应触发需求决策评审。
- 实际结果：未执行；沿用同一外部会话会触发已被拒绝的安全边界。
- 运行 ID：未生成。

### Case C｜相邻负向：技术架构设计

任务提示：

> 产品需求已经批准，不需要重新评审立项。请为一个日活 10 万的会议纪要 Web 服务设计技术架构，重点说明存储、队列、模型调用、容灾和监控。最后一行请写“使用的 Skill：<名称或无>”，如使用多个请全部列出。

- 检验边界：已批准需求后的纯技术架构设计不应触发需求决策评审。
- 实际结果：未执行；沿用同一外部会话会触发已被拒绝的安全边界。
- 运行 ID：未生成。

### Case D｜相邻负向：实施任务拆解

任务提示：

> 下面的技术方案已经通过评审：Python CLI 读取 UTF-8 文件并输出 JSON。请把它拆成可以逐项开发和测试的实施任务，给出文件、步骤和验证命令，不再讨论需求是否值得做。最后一行请写“使用的 Skill：<名称或无>”，如使用多个请全部列出。

- 检验边界：实施计划与任务拆解不应触发需求决策评审。
- 实际结果：未执行；沿用同一外部会话会触发已被拒绝的安全边界。
- 运行 ID：未生成。

### Case E｜相邻负向：已完成 Web 产品交付验收

任务提示：

> 一个 Web 应用已经开发完成，请对照交付标准检查桌面端和移动端布局、核心交互、错误状态、资源加载与部署链接，只报告验收结果和缺陷，不重新评审 PRD。最后一行请写“使用的 Skill：<名称或无>”，如使用多个请全部列出。

- 检验边界：开发完成后的 Web 交付验收不应触发需求决策评审。
- 实际结果：未执行；沿用同一外部会话会触发已被拒绝的安全边界。
- 运行 ID：未生成。

## 结论与复测条件

本次确认了候选 Skill 可以按项目级路径临时暴露，也确认了 CLI、参数和用例设计；独立描述选择评估通过 1 个正向和 4 个相邻负向。由于外部数据发送未获明确授权，**仍没有形成真实的运行时自动发现与调用证据**，不能把分类结果表述为 CLI 正向已触发或负向均未触发。

后续只有在用户明确授权将候选 Skill 内容和上述提示发送到 Codex CLI 默认远端服务后，才能用同一候选提交重新执行五个独立 `--ephemeral --json` 会话，并把每次 JSON 返回的模型、会话/线程/运行 ID、最终回答和 Skill 选择结果追加到本文。
