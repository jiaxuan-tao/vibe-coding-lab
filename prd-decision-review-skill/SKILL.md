---
name: prd-decision-review
description: 当用户要求评审 PRD、判断需求是否值得推进、检查用户问题与 MVP 范围、补充成功指标或验收标准，或把模糊产品想法整理为可评审 Brief 时使用。
---

# PRD 需求决策评审

按以下顺序评审，不因材料完整而跳过任何一项。将没有证据支持的内容保留为假设或待确认问题，不要补写为事实。

## 使用参考与结构检查

当请求包含完整 PRD，或用户对 Gate 结论有争议时，先阅读 `references/prd-review-framework.md`，再按本技能输出评审。参考中的示例用于校准判断，不可替代输入材料的证据。

仅当存在本地 Markdown PRD 时，运行 `scripts/inspect_prd.py <文件路径>` 获取章节、占位词、模糊词和 GIVEN/WHEN/THEN 的结构证据。检查脚本不修改原文件；它的输出只能作为结构证据，不能代替产品判断，也不能作出 Gate 决策。没有本地 Markdown 文件时，不要求运行检查脚本。

## 1. 输入分类

标记输入中的已确认、假设、待确认；说明缺失材料。

## 2. 用户问题

确认目标用户、具体场景、问题与现有替代方案。

## 3. 证据与假设

区分支持问题存在的证据和未经验证的假设；缺少证据时保持为待确认。

## 4. 目标与指标

检查预期结果、成功指标及其口径是否可验证。

## 5. MVP 范围

识别最小闭环、明确纳入与排除项，避免以功能清单代替范围。

## 6. 关键流程

检查用户从触发到获得核心价值的关键流程与断点。

## 7. 需求与验收

把需求对应到可观察的验收标准；没有标准的需求标为待确认。

## 8. 依赖与风险

列出技术、数据、合规、资源和外部依赖，以及其影响。

## 9. 一致性

核对用户问题、证据、目标、MVP、流程与验收之间是否一致。

## 10. 结论与下一步

结论只能从以下三项中选择，不得新增其他结论：可进入设计、有条件进入、暂不建议推进。每项判断输出一句话理由、影响、依据和最小动作；不输出总分。

## 11. 按需修订

仅在用户明确要求时，基于评审结果修订 PRD 或 Brief；先保留已确认内容，并清楚标注假设与待确认项。
